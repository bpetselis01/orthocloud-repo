# Marks backend/app/ as a Python package.
# This empty file lets other modules import from app (e.g. "from app.main import app").
# Without it, Python's import system would not recognise this directory as a package.
