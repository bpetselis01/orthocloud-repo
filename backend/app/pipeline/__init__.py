# Marks backend/app/pipeline/ as a Python package.
# This package will hold the DICOM-to-NIfTI conversion and TotalSegmentator
# inference logic added in Steps 2 and 3. Empty for now (Step 1 scaffold only).
